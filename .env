# CPS Production Management System — Local Environment
# Copy values from .env.example and fill in real secrets/endpoints before running

# cps-api Backend Base URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
